name: Sandbox

on:
  push:
    branches-ignore:
      - main

jobs:
  Plan:
    name: "Plan"
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install AWS CLI
        run: echo "Rodar somente o plano Sandbox > tf-cicd-stg-push.yaml"
